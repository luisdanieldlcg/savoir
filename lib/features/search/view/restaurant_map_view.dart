import 'package:flutter/material.dart';

class RestaurantMapView extends StatelessWidget {
  const RestaurantMapView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Restaurant Map View'),
    );
  }
}
