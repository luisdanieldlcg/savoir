import 'package:flutter/material.dart';

class HistoryBookingsTab extends StatelessWidget {
  const HistoryBookingsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('No se encontraron reservas anteriores'),
    );
  }
}
